"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.RequestBlocker = void 0;
const UrlAccessorResolver_1 = require("./common/urlAccessor/UrlAccessorResolver");
class RequestBlocker {
    constructor() {
        this.urlsToBlock = [];
    }
    shouldBlockRequest(request, matcher) {
        let urlAccessor = UrlAccessorResolver_1.UrlAccessorResolver.getUrlAccessor(request);
        for (let urlToBlock of this.urlsToBlock) {
            if (matcher(urlAccessor.getUrlFromRequest(request), urlToBlock)) {
                return true;
            }
        }
        return false;
    }
    addUrlsToBlock(urls) {
        this.urlsToBlock = this.urlsToBlock.concat(urls);
    }
    clearUrlsToBlock() {
        this.urlsToBlock = [];
    }
}
exports.RequestBlocker = RequestBlocker;
